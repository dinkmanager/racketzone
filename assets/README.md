# Client assets

Per-client branding lives under this folder:

```
assets/
  racketzone/
    logo.png
    court.jpg        # indoor court photo used on the display board
    payment-qr.png   # optional — add when GCash QR is ready
```
